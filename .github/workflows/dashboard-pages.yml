name: Deploy meta explorer

on:
  push:
    branches:
      - historical-online-chunked
    paths:
      - "dashboard/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "meta-explorer-pages"
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload explorer
        uses: actions/upload-pages-artifact@v3
        with:
          path: dashboard

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
